export interface OasthOptions {
    options: {
        /* Whenever to reuse the same session to make a request. If set to false, will randomize it in each request. */
        reuseSessions: boolean
    }
}
